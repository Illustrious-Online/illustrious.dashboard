{
  "name": "api",
  "version": "1.0.50",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "build": "bun build --compile --minify-whitespace --minify-syntax --target bun --outfile ./dist/index.js ./src/index.ts",
    "start": "bun start src/index.ts"
  },
  "dependencies": {
    "elysia": "latest"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "bun-types": "^0.5.0"
  },
  "type":"module",
  "module": "src/index.js",
  "engines": {
    "node": ">=16.0.0"
  }
}